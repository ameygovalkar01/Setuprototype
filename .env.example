# Setu Environment Configuration Template
# Generate your bcrypt hash using: python generate_hash.py

ADMIN_PASSWORD_HASH="b..."
APP_ENV=development

# SMTP Email Dispatcher (For real email delivery to Gmail / Outlook / etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=Setu Portal (MoSJE)
